import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'finn.app.pricetrol',
  appName: 'pricetrol',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
